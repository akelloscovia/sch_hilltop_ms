# Copy this to .env and fill values before running migrations or deploying
# Use Supabase or Neon connection string for DATABASE_URL

FLASK_ENV=production
DATABASE_URL=postgres://<user>:<password>@<host>:5432/<database>
JWT_SECRET_KEY=replace-with-a-strong-random-secret
ADMIN_EMAIL=admin@hilltopjunior.com
ADMIN_PASSWORD=ChangeMeStrongly!
MGNT_WEBHOOK_SECRET=replace-webhook-secret
BREVO_API_KEY=
SENDER_EMAIL=
SENDER_NAME=Hilltop Junior School
UPLOAD_FOLDER=
UPLOAD_FOLDER_ABOUT=
CORS_ORIGINS=http://localhost:3000,https://hilltopjunior.com,https://admin.hilltopjunior.com
CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>  # optional for persistent uploads

# Optional defaults used by seed script
DEFAULT_ADDRESS=Kasangati, Wakiso District, Uganda
DEFAULT_PHONE=+256 771 234 567
DEFAULT_EMAIL=info@hilltopjunior.ug
DEFAULT_WORKING_HOURS=Monday - Friday: 7:00 AM - 5:00 PM

# Notes:
# 1) Keep this file private (do NOT commit secrets to public repos).
# 2) After filling, run the migrations+seed script (see README):
#    cd mgnt
#    . .venv\Scripts\Activate.ps1   # PowerShell (Windows)
#    pip install -r requirements.txt
#    python scripts/migrate_and_seed.py
# 3) If deploying to Render, set the same env vars in the service's Environment panel.
